cpv() {
  rsync                                   \
    --perms                               \
    --owner                               \
    --group                               \
    --backup                              \
    --backup-dir="/tmp/rsync-${USERNAME}" \
    --recursive                           \
    -hhh                                  \
    --progress                            \
    "$@"
}
