# The following plugins should only be sourced if
# NOT logged in via ssh/login shell

# Reminder to use the aliases you have set
djui/alias-tips

# Provides a function 'z' that allows you to cd to your
# most cd-ed to directories
agkozak/zsh-z

# Standardizes macOS' 'pbcopy' and 'pbpaste' commands to
# work as expected regardless of whether you are on Linux or macOS
# (Linux dependencies: xdg-open, xclip)
zpm-zsh/clipboard

# Bookmark your most commonly cd-ed to directories
# (Not currently being used because I already have a
# shell bookmark mechanism in my Zsh config)
# zpm-zsh/bookmarks

# Syntax highlighting at the command line
# (must be loaded before zsh-users/zsh-history-substring-search)
zdharma/fast-syntax-highlighting

# Zsh port of Fish history search
zsh-users/zsh-history-substring-search

# Automatically suggest commands to run based on your history
zsh-users/zsh-autosuggestions

# More interactive Ctrl-R reverse history search
psprint/history-search-multi-word
