# macOS cruft
**/.DS_Store

# Unecessary Python artifacts
**/__pycache__

# Call your Python virtualenv 'env'
env

# The result symlink created on NixOS
result

# When dotdrop has to deploy files that use Jinja
# templates, they are stored in this directory, so
# we don't need to check that into Git.
templated

# Some variables we don't want checked into Git, so
# dotdrop will prompt for their values upon dotfile
# deployment and store them in this file.
uservariables.yaml

# These directories store scripts that provide shell tab
# completions for Zsh and can be generated from external programs,
# so since those can be re-generated from said programs we
# don't need to check this into Git either.
net.sourceforge.Zsh/*/zfunc/_*
